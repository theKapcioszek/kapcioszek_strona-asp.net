@page
@model Kapcioszek_strona.Pages.album_bootleg_princessModel
@{
    ViewData["Title"] = "Bootleg Princess";
    ViewData["css"] = "style2.css";
    ViewData["favicon"] = "../img/channels4_profile_2.png";
}

    <div class="body">
        <div>
            <h2>Bootleg Princess</h2>
        </div>
        <div>
            <table>
                @foreach (int item in Model.id)
                {
                    <tr>
                        <td>
                            @Model.name[item]
                        </td>
                        <td>
                            <iframe width="360" height="215" src="@Model.link[item]" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
                        </td>
                    </tr>
                }
            </table>
        </div>
    </div>


